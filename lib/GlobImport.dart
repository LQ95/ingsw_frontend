import 'package:flutter/material.dart';
var GlobalAppBar=
AppBar(
actions: <Widget>[
          IconButton(
              icon: const Icon(Icons.settings),
              onPressed: () => {},
                    )
                  ]
      );